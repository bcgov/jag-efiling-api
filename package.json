{
    "name": "e-filing-api",
    "version": "1.0.0",
    "private": false,
    "dependencies": {
        "body-parser": "^1.18.2",
        "express": "^4.16.3",
        "morgan": "^1.9.0",
        "pg": "^7.4.1",
        "querystring": "^0.2.0",
        "soap": "^0.24.0",
        "socket.io": "^2.0.4",
        "url": "^0.11.0",
        "yop-postgresql": "^1.1.0",
        "yop-promises": "^1.1.0"
    },
    "devDependencies": {
        "chai": "latest",
        "istanbul": "latest",
        "jsdom": "latest",
        "log4js": "latest",
        "mocha": "latest",
        "request": "2.81.0"
    },
    "engines": {
        "node": ">=6.14.0",
        "npm": ">=3.10.10"
    },
    "scripts": {
        "pretest": "dropdb --if-exists $PGDATABASE -U postgres; createdb $PGDATABASE -U postgres",
        "test": "npm run external-tests",
        "start": "node start.js",
        "external-tests": "mocha tests/external --recursive --exit --timeout 0",
        "internal-tests": "mocha tests/internal/*.spec.js tests/support/*.spec.js tests/internal/hub/*.spec.js --recursive --exit",
        "test:coverage": "istanbul cover node_modules/mocha/bin/_mocha -- tests/* app/*"
    }
}
